# Report Template Guidance

Keep the final decision objective:
- GO only if all critical endpoints pass.
- NO-GO if health fails or routes are unavailable.

Use strict pass/fail, not optimistic language.
