# Demo Checklist (No Ollama)

- Backend starts in Docker without Ollama dependency.
- Health endpoint is stable.
- Core endpoints return response (not route missing).
- UI path for demo is defined (what to show first, fallback message prepared).
